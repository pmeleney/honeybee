# Core
numpy>=1.23
matplotlib>=3.6
scipy>=1.9
scikit-learn>=1.1
pandas>=1.5

# TensorFlow: choose one of the following depending on platform
# Default CPU (Linux/Windows, some Intel macs)
tensorflow>=2.12; platform_system!="Darwin" or platform_machine!="arm64"
# Apple Silicon (macOS arm64)
tensorflow-macos>=2.12; platform_system=="Darwin" and platform_machine=="arm64"
tensorflow-metal>=0.8; platform_system=="Darwin" and platform_machine=="arm64"

# NEAT (optional)
neat-python>=0.92

